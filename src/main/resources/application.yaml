spring:
  application:
    name: shop-with-me
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 900 # 15Mins
    refreshTokenExpiration: 604800 #7days
  profiles:
    active: prod
stripe:
  secretKey: ${STRIPE_SECRET_KEY}
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}
